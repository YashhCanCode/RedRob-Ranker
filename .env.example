# This project needs NO API keys to produce a submission — the ranking step is
# fully offline. Keys are only relevant if you (optionally) use an LLM to
# regenerate role_spec.yaml during pre-computation. Leave blank otherwise.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
